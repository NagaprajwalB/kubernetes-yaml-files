# NOTE: Secrets are base64-encoded, NOT encrypted, by default.
# Enable etcd encryption at rest and/or use an external secret manager for production.
apiVersion: v1
kind: Secret
metadata:
  name: db-credentials
type: Opaque
stringData:
  username: appuser
  password: change-me-in-production
